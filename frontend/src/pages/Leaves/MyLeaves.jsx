import React from 'react';

// Legacy placeholder kept only so Tailwind/Vite content scanning
// can safely read this path. The real implementation for
// list, calendar, and personal views now lives in
// `src/pages/Leaves/index.jsx` and related components.
// This component is not used anywhere in routing.

const MyLeaves = () => {
  return null;
};

export default MyLeaves;


